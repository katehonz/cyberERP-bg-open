# 🤝 Допринасяне към Cyber ERP

Благодарим Ви за интереса към Cyber ERP! Това ръководство ще Ви помогне да се включите в разработката.

---

## 📋 Съдържание

- [Код на поведение](#код-на-поведение)
- [Как да започна](#как-да-започна)
- [Процес на разработка](#процес-на-разработка)
- [Стил на кода](#стил-на-кода)
- [Pull Request процес](#pull-request-процес)
- [Често срещани въпроси](#често-срещани-въпроси)

---

## 🌟 Код на поведение

### Наши принципи

- **Уважение** - Третирайте всички с уважение и учтивост
- **Инклузивност** - Приветстваме хора от всички среди
- **Откритост** - Дръжте се отворени към различни гледни точки
- **Сътрудничество** - Работете заедно за по-добри резултати

### Какво се очаква от Вас

- Бъдете уважителни в комуникацията (GitHub issues, PRs, comments)
- Помагайте на новодошлите да се ориентират
- Фокусирайте се върху това, което е най-добро за общността
- Показвайте емпатия към другите членове

---

## 🚀 Как да започна

### 1. Fork проекта

```bash
# 1. Fork-нете репозиторита на GitHub
# 2. Clone-нете Вашия fork
git clone https://github.com/YOUR_USERNAME/cyberERP-open-bg.git
cd cyberERP-open-bg
```

### 2. Настройте upstream

```bash
git remote add upstream https://github.com/ORIGINAL_OWNER/cyberERP-open-bg.git
```

### 3. Инсталирайте зависимостите

```bash
mix deps.get
cd apps/cyber_web/assets && npm install && cd ../../..
```

### 4. Настройте база данни

```bash
mix ecto.create
mix ecto.migrate
```

### 5. Стартирайте сървъра

```bash
mix phx.server
```

---

## 🔄 Процес на разработка

### 1. Създайте клон за Вашата промяна

```bash
git checkout -b feature/your-feature-name
# или за bugfix:
git checkout -b fix/issue-123-bug-description
```

#### Конвенции за именуване на клонове:

- `feature/` - за нови функционалности
- `fix/` - за поправки на грешки
- `refactor/` - за рефакторинг
- `docs/` - за промени в документацията
- `test/` - за добавяне на тестове
- `chore/` - за други промени

### 2. Направете промените

```bash
# Направете своите промени...
# Форматирайте кода
mix format

# Пуснете тестовете
mix test

# Проверете кода
mix credo
```

### 3. Commit вашите промени

#### Конвенция за commit messages:

Използваме [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<scope>): <subject>

<body>

<footer>
```

#### Type:

- `feat`: Нова функционалност
- `fix`: Поправка на грешка
- `docs`: Промени в документацията
- `style`: Промени в стила (не променят логиката)
- `refactor`: Рефакторинг
- `test`: Добавяне на тестове
- `chore`: Други промени

#### Примери:

```bash
# Нова функционалност
git commit -m "feat(accounting): add VAT calculation for invoices"

# Поправка
git commit -m "fix(inventory): correct stock level calculation"

# Документация
git commit -m "docs: update API documentation for sales module"

# Рефакторинг
git commit -m "refactor(auth): simplify user validation logic"
```

### 4. Push към Вашия fork

```bash
git push origin feature/your-feature-name
```

### 5. Създайте Pull Request

1. Отидете на GitHub и създайте PR
2. Попълнете шаблона за PR
3. Изчакайте review

---

## 📝 Стил на кода

### Elixir

```elixir
# ✅ Добре
def calculate_total(invoice) do
  invoice.lines
  |> Enum.map(& &1.price * &1.quantity)
  |> Enum.sum()
  |> Decimal.from_float()
end

# ❌ Лошо
def calculate_total(invoice) do
  total = 0
  Enum.each(invoice.lines, fn line ->
    total = total + line.price * line.quantity
  end)
  total
end
```

#### Правила:

1. **Модули** - Използвайте `use` statement за модули
2. **Pipe operator** - Използвайте `|>` за chaining на операции
3. **Pattern matching** - Предпочитайте pattern matching пред `if`
4. **Модулни имена** - Използвайте PascalCase за модули, snake_case за функции
5. **Дължина на реда** - Максимум 120 символа
6. **Spaces** - 2 spaces indent, няма tabs

### LiveView

```elixir
# ✅ Добре
def handle_event("save", %{"invoice" => params}, socket) do
  case Invoices.create_invoice(params) do
    {:ok, invoice} ->
      {:noreply, put_flash(socket, :info, "Фактурата е създадена!")}

    {:error, changeset} ->
      {:noreply, assign(socket, :changeset, changeset)}
  end
end
```

### Frontend (React + Tailwind)

```jsx
// ✅ Добре
function InvoiceForm({ invoice, onSubmit }) {
  const [formData, setFormData] = useState(invoice);

  const handleSubmit = (e) => {
    e.preventDefault();
    onSubmit(formData);
  };

  return (
    <form onSubmit={handleSubmit} className="space-y-4">
      {/* ... */}
    </form>
  );
}

// ❌ Лошо
function InvoiceForm(props) {
  var data = props.invoice;
  // ...
}
```

#### Правила:

1. **Components** - Functional components с hooks
2. **Tailwind** - Utility-first classes, избягвайте custom CSS
3. **Naming** - PascalCase за компоненти, camelCase за променливи
4. **Props** - Деструктурирайте props в началото

---

## 🔄 Pull Request процес

### Преди да създадете PR

- [ ] Кодът е форматиран (`mix format`)
- [ ] Всички тестове минават (`mix test`)
- [ ] Няма Credo warnings (`mix credo`)
- [ ] Добавени са тестове за новата функционалност
- [ ] Документацията е актуализирана
- [ ] Commit messages следват конвенцията

### PR Template

```markdown
## Описание
Кратко описание на промените.

## Тип на промяна
- [ ] Bugfix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation update

## Свързани issues
Closes #123

## Проверки
- [ ] Тестове добавени/актуализирани
- [ ] Документация актуализирана
- [ ] Кодът следва стила на проекта

## Скрийншоти (ако е приложимо)
<!-- Добавете скрийншоти за UI промени -->
```

### Code Review процес

1. **Автоматични проверки** - CI tests минават
2. **Review** - поне един maintainer ревюира PR
3. **Changes** - ако са нужни промени, правете ги в същия клон
4. **Approval** - след approval, PR ще бъде merger-нат

---

## ✅ Checklist преди Submit

### Код

- [ ] Кодът работи според очакванията
- [ ] Следва конвенциите за стил
- [ ] Липсват logging или debug statements
- [ ] Липсват commented-out код

### Тестове

- [ ] Новите функционалности имат тестове
- [ ] Съществуващите тестове минават
- [ ] Тестовете покриват edge cases

### Документация

- [ ] README.md е актуализиран (ако е нужно)
- [ ] API документацията е обновена
- [ ] Commit messages са ясни и описателни

---

## ❓ Често срещани въпроси

### Какво да правя, ако CI tests се провалят?

Проверете лога, поправете проблема и push-нете нов commit към същия клон.

### Мога ли да правя промени в няколко файла в един PR?

Да, но ако промените са в различни модули или функционалности, е по-добре да ги разделите.

### Как да тествам визуални промени?

След като стартирате `mix phx.server`, отворете http://localhost:4000 и проверете UI промените.

### Как да създам нов модул?

Вижте [Модулен план](docs/MODULES_PLAN.md) и следвайте съществуващите примери в `apps/cyber_core/`.

---

## 📚 Ресурси

- [Phoenix Documentation](https://hexdocs.pm/phoenix)
- [Elixir Style Guide](https://github.com/lexmag/elixir-style-guide)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [How to Write a Git Commit Message](https://chris.beams.io/posts/git-commit/)

---

## 🙏 Благодарим

Вашият принос е ценен за нас! Благодарим Ви, че отделяте време да помогнете на Cyber ERP да се развива.

---

**За въпроси:** Отворете issue или ни пишете на [GitHub Discussions](https://github.com/...) (попълнете URL)
