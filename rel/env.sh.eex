#!/bin/sh

# Sets and samples the PHX_SERVER environment variable
# to enable/disable the Phoenix server on release boot
export PHX_SERVER=true

# Configure release to run migrations on start
export RELEASE_COOKIE="${RELEASE_COOKIE:-cyber_erp_cookie}"
